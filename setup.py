#!/usr/bin/env python

from setuptools import setup

setup(name='plmap3',
      version='1.0.0',
      description='Parallel map using multi-threading and multi-processing for python3',
      author='Lohith B R',
      packages=['plmap3'],
      license = "MIT",
      keywords = "parallelization plmap plmapp plmapt pmapp multi-threading multi-processing parallel map python3",
      url = "https://github.com/brlohith/parallelization3",
    #   install_requires=[
    #       'queue',
    #   ]
     )


